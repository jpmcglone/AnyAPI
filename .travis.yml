os: osx
osx_image: xcode11.2
language: objective-c

install:
  - set -o pipefail
  - cd Example
  - gem install bundler
  - bundle install
  - bundle exec pod deintegrate
  - bundle exec pod install
script:
  - bundle exec fastlane tests
